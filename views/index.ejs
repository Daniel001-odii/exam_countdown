<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta property="og:title" content="Countdown to D-Day!" />
  
  <meta property="og:image" content="https://exam-countdown.onrender.com/og-image.png" />
  <meta property="og:description" content="Time left until the event!" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Anton&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

  <title>Countdown to FUTO school exams</title>
</head>
<body>
  <h1 class="title_">EXAM</h1>
  <span style="margin: 0px; font-size: 14px; color: #0768b8; text-align: center;">...Lol, you still have time to check countdown?</span>
  <div id="countdown" class="countdown_container">Loading...</div>
  <!-- <p>Target day: <%= targetDate.toISOString() %></p> -->

  <img class="scared_image" src="https://raw.githubusercontent.com/Daniel001-odii/odiiDaniel/refs/heads/main/images/scared.png">
  <div class="footer"><span> Inspired by exam fear with ❤️ from <a href="https://x.com/odiidanny">dan_</a>& Noni</span></div>

  <script>
    // Set the target date
    const targetDate = new Date("<%= targetDate.toISOString() %>");

    function updateCountdown() {
      const now = new Date();
      const diff = targetDate - now;

      if (diff <= 0) {
        document.getElementById('countdown').innerHTML = "The event has started!";
        clearInterval(intervalId);  // Stop updating once the countdown reaches zero
        return;
      }

      // Calculate time components
      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
      const minutes = Math.floor((diff / (1000 * 60)) % 60);
      const seconds = Math.floor((diff / 1000) % 60);

      // Display the countdown
      document.getElementById('countdown').innerHTML = 
        // `Days: ${days} | Hours: ${hours} | Minutes: ${minutes} | Seconds: ${seconds}`;
        `<div class='line'><div class='sub_line'><span class='number'> ${days} </span><span class='subtitle'>Days</span></div> <div class='sub_line'><span class='number'>${hours}</span><span class='subtitle'>Hours</span></div><div class='sub_line'><span class='number'>${minutes}</span><span class='subtitle'>Minutes</span></div> <div class='sub_line'><span class='number'>${seconds}</span><span class='subtitle'>Seconds</span></div><div/>`;
    }

    // Update countdown every second
    const intervalId = setInterval(updateCountdown, 1000);
    updateCountdown();  // Initial call to display immediately
  </script>


<style>
  *{
    box-sizing: border-box;
  }
  a{
    text-decoration: none;
    color: inherit;
  }
  html, body{
    margin: 0;
    padding: 0;
    font-family: "Poppins", sans-serif;
  }
  .poppins-regular {
    font-family: "Poppins", sans-serif;
    font-weight: 400;
    font-style: normal;
  }

  body{
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: rgb(7,104,184);
    background: linear-gradient(0deg, rgba(7,104,184,1) 0%, rgba(0,0,0,1) 15%);
    color: white;
  }


  .title_{
    margin-top: -60px;
    margin-bottom: 0px;
    font-size: 60px;
    font-weight: 450;
  }
  .line{
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    gap: 10px;
  }

  .sub_line{
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 15px;
    padding-bottom: 0px;
    margin-bottom: 0px;
    /* border: 1px solid red; */
  }

  .number{
    margin: 0px;
    font-weight: bolder;
  }

  .subtitle{
    padding: 0px;
    margin: 0px;
    color: #ffffff86;
    font-size: 15px !important;
  }

  .countdown_container{
    margin-top: 30px;
    padding: 10px;
    display: flex;
    flex-direction: row;
    font-size: 55px;
  }

  .scared_image{
    position: fixed;
    top: -40px;
    /* top: 50vh; */
    left: -30px;
    opacity: 0.3;
    transform: rotate(30deg);
  }

  .footer{
    padding: 10px 40px;
    /* border: 1px solid #ffffff4a; */
    /* border-radius: 50px; */
    /* background: #ffffff32; */
    font-size: 14px;
    position: fixed;
    bottom: 10px;
    /* color: #ffffff74; */
  }
</style>
</body>
</html>
